import {Section} from "Type/Section.tsx";

type ContactData = {
  headerOne: string;
  descriptionOne: string;
  email: string;
  phone: string;
  sections: Section[];
};

export default ContactData;
